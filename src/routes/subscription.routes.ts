import { Router } from "express";


const subscription = Router();

subscription.get("/", )
subscription.get("/:id", )
subscription.post("/", )
subscription.put("/:id", )
subscription.delete("/:id", )